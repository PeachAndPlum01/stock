# 🔐 登录功能测试指南

## 📋 目录
1. [启动准备](#启动准备)
2. [启动服务](#启动服务)
3. [测试登录](#测试登录)
4. [登录流程详解](#登录流程详解)
5. [常见问题排查](#常见问题排查)

---

## 🚀 启动准备

### 1. 确保基础环境已安装
```bash
# 检查Java版本（需要JDK 8+）
java -version

# 检查Maven版本
mvn -version

# 检查Node.js版本（需要14+）
node -v

# 检查npm版本
npm -v
```

### 2. 确保数据库已初始化
```bash
# 登录MySQL
mysql -u root -p

# 执行初始化脚本
source /Users/lifeng/Desktop/code/stock/sql/init.sql

# 验证数据
USE stock_investment;
SELECT * FROM sys_user;
```

**预期结果**：应该看到2个用户
```
+----+----------+----------------------------------+----------+-------------------+-------+--------+---------------------+---------------------+
| id | username | password                         | nickname | email             | phone | status | create_time         | update_time         |
+----+----------+----------------------------------+----------+-------------------+-------+--------+---------------------+---------------------+
|  1 | admin    | e10adc3949ba59abbe56e057f20f883e | 管理员   | admin@example.com | NULL  |      1 | 2026-02-01 20:00:00 | 2026-02-01 20:00:00 |
|  2 | test     | e10adc3949ba59abbe56e057f20f883e | 测试用户 | test@example.com  | NULL  |      1 | 2026-02-01 20:00:00 | 2026-02-01 20:00:00 |
+----+----------+----------------------------------+----------+-------------------+-------+--------+---------------------+---------------------+
```

### 3. 确保Redis和RabbitMQ已启动
```bash
# 启动Redis
redis-server

# 验证Redis
redis-cli ping
# 应该返回：PONG

# 启动RabbitMQ（如果使用Docker）
docker start rabbitmq
```

---

## 🎯 启动服务

### 方式一：使用启动脚本（推荐）

```bash
cd /Users/lifeng/Desktop/code/stock

# 启动所有后端服务
./start-all.sh

# 等待所有服务启动完成（约30-60秒）
```

**启动顺序**：
1. ✅ 认证服务 (8081端口)
2. ✅ 数据服务 (8082端口)
3. ✅ 投资服务 (8083端口)
4. ✅ 关联服务 (8084端口)
5. ✅ API网关 (8080端口)

### 方式二：手动启动

#### 1. 启动认证服务
```bash
cd /Users/lifeng/Desktop/code/stock/stock-auth-service
mvn clean package -DskipTests
java -jar target/stock-auth-service-1.0.0.jar
```

#### 2. 启动API网关
```bash
cd /Users/lifeng/Desktop/code/stock/stock-gateway
mvn clean package -DskipTests
java -jar target/stock-gateway-1.0.0.jar
```

#### 3. 启动前端
```bash
cd /Users/lifeng/Desktop/code/stock/frontend
npm install
npm run dev
```

### 验证服务启动

```bash
# 检查认证服务
curl http://localhost:8081/actuator/health

# 检查网关
curl http://localhost:8080/actuator/health

# 检查前端
curl http://localhost:5173
```

---

## 🧪 测试登录

### 1. 打开浏览器访问前端

```
http://localhost:5173
```

### 2. 使用测试账号登录

#### 测试账号1：管理员
- **用户名**：`admin`
- **密码**：`123456`

#### 测试账号2：普通用户
- **用户名**：`test`
- **密码**：`123456`

### 3. 登录步骤

1. 在登录页面输入用户名和密码
2. 点击"登录"按钮
3. 等待验证（约1-2秒）
4. 登录成功后自动跳转到首页（中国地图页面）

### 4. 验证登录成功

**前端验证**：
- ✅ 页面跳转到 `/home`
- ✅ 浏览器控制台无错误信息
- ✅ 可以看到中国地图
- ✅ LocalStorage中存储了Token

**后端验证**：
```bash
# 查看认证服务日志
tail -f /Users/lifeng/Desktop/code/stock/stock-auth-service/logs/app.log

# 查看Redis中的Token
redis-cli
> KEYS token:*
> GET token:1
```

---

## 📖 登录流程详解

### 完整流程图

```
┌─────────┐
│  用户   │
└────┬────┘
     │ 1. 输入用户名/密码
     ▼
┌─────────────────┐
│  前端 (Vue3)    │
│  localhost:5173 │
└────┬────────────┘
     │ 2. POST /api/auth/login
     │    { username, password }
     ▼
┌─────────────────┐
│  API网关        │
│  localhost:8080 │
└────┬────────────┘
     │ 3. 路由转发
     │    → http://localhost:8081/auth/login
     ▼
┌─────────────────┐
│  认证服务       │
│  localhost:8081 │
└────┬────────────┘
     │ 4. 查询数据库
     ▼
┌─────────────────┐
│  MySQL          │
│  sys_user表     │
└────┬────────────┘
     │ 5. 返回用户信息
     ▼
┌─────────────────┐
│  认证服务       │
│  - MD5验证密码  │
│  - 生成JWT Token│
│  - 存储到Redis  │
└────┬────────────┘
     │ 6. 返回Token和用户信息
     ▼
┌─────────────────┐
│  前端           │
│  - 保存Token    │
│  - 保存用户信息 │
│  - 跳转首页     │
└─────────────────┘
```

### 关键代码位置

#### 1. 前端登录页面
**文件**：`frontend/src/views/Login.vue`

```javascript
const handleLogin = async () => {
  // 表单验证
  await loginFormRef.value.validate(async (valid) => {
    if (valid) {
      loading.value = true
      try {
        // 调用登录API
        const res = await login(loginForm)
        
        // 保存Token和用户信息
        userStore.setToken(res.data.token)
        userStore.setUserInfo(res.data.userInfo)
        
        // 跳转首页
        router.push('/home')
      } catch (error) {
        console.error('登录失败：', error)
      }
    }
  })
}
```

#### 2. 前端API调用
**文件**：`frontend/src/api/index.js`

```javascript
export const login = (data) => {
  return request({
    url: '/auth/login',
    method: 'post',
    data
  })
}
```

#### 3. 网关路由配置
**文件**：`stock-gateway/src/main/resources/application.yml`

```yaml
spring:
  cloud:
    gateway:
      routes:
        - id: auth-service
          uri: http://localhost:8081
          predicates:
            - Path=/auth/**
```

#### 4. 认证服务Controller
**文件**：`stock-auth-service/src/main/java/com/stock/auth/controller/AuthController.java`

```java
@PostMapping("/login")
public ResponseEntity<Map<String, Object>> login(@RequestBody Map<String, String> request) {
    String username = request.get("username");
    String password = request.get("password");
    
    Map<String, Object> result = authService.login(username, password);
    
    Map<String, Object> response = new HashMap<>();
    response.put("code", 200);
    response.put("message", "登录成功");
    response.put("data", result);
    
    return ResponseEntity.ok(response);
}
```

#### 5. 认证服务Service
**文件**：`stock-auth-service/src/main/java/com/stock/auth/service/AuthService.java`

```java
public Map<String, Object> login(String username, String password) {
    // 1. 查询用户
    User user = userMapper.selectOne(
        new LambdaQueryWrapper<User>()
            .eq(User::getUsername, username)
    );
    
    // 2. 验证密码
    String encryptedPassword = encryptPassword(password);
    if (!user.getPassword().equals(encryptedPassword)) {
        throw new RuntimeException("密码错误");
    }
    
    // 3. 生成Token
    String token = jwtUtil.generateToken(user.getId(), user.getUsername());
    
    // 4. 存储到Redis
    redisTemplate.opsForValue().set(
        "token:" + user.getId(), 
        token, 
        24, 
        TimeUnit.HOURS
    );
    
    // 5. 返回结果
    Map<String, Object> result = new HashMap<>();
    result.put("token", token);
    result.put("userId", user.getId());
    result.put("username", user.getUsername());
    result.put("nickname", user.getNickname());
    
    return result;
}
```

---

## 🔧 常见问题排查

### 问题1：前端无法访问

**现象**：浏览器打开 `http://localhost:5173` 无响应

**排查步骤**：
```bash
# 1. 检查前端是否启动
cd /Users/lifeng/Desktop/code/stock/frontend
npm run dev

# 2. 检查端口占用
lsof -i :5173

# 3. 查看前端日志
# 在启动前端的终端查看错误信息
```

**解决方案**：
- 确保Node.js版本 >= 14
- 删除 `node_modules` 重新安装：`rm -rf node_modules && npm install`
- 检查 `vite.config.js` 配置

---

### 问题2：登录时提示"用户不存在"

**现象**：输入 `admin/123456` 后提示用户不存在

**排查步骤**：
```bash
# 1. 检查数据库
mysql -u root -p
USE stock_investment;
SELECT * FROM sys_user WHERE username = 'admin';

# 2. 检查认证服务日志
tail -f /Users/lifeng/Desktop/code/stock/stock-auth-service/logs/app.log
```

**解决方案**：
```sql
-- 如果用户不存在，重新插入
INSERT INTO `sys_user` (`username`, `password`, `nickname`, `email`, `status`) 
VALUES ('admin', 'e10adc3949ba59abbe56e057f20f883e', '管理员', 'admin@example.com', 1);
```

---

### 问题3：登录时提示"密码错误"

**现象**：用户存在但密码验证失败

**排查步骤**：
```bash
# 1. 验证密码加密
echo -n "123456" | md5
# 应该输出：e10adc3949ba59abbe56e057f20f883e

# 2. 检查数据库中的密码
mysql -u root -p
USE stock_investment;
SELECT username, password FROM sys_user WHERE username = 'admin';
```

**解决方案**：
```sql
-- 更新密码为正确的MD5值
UPDATE sys_user 
SET password = 'e10adc3949ba59abbe56e057f20f883e' 
WHERE username = 'admin';
```

---

### 问题4：登录后无法跳转

**现象**：登录成功但停留在登录页

**排查步骤**：
```bash
# 1. 打开浏览器开发者工具（F12）
# 2. 查看Console标签页的错误信息
# 3. 查看Network标签页的请求响应
```

**解决方案**：
- 检查前端路由配置：`frontend/src/router/index.js`
- 检查用户Store：`frontend/src/store/user.js`
- 清除浏览器缓存和LocalStorage

---

### 问题5：网关无法转发请求

**现象**：前端请求 `/api/auth/login` 返回404

**排查步骤**：
```bash
# 1. 检查网关是否启动
curl http://localhost:8080/actuator/health

# 2. 检查认证服务是否启动
curl http://localhost:8081/actuator/health

# 3. 直接访问认证服务
curl -X POST http://localhost:8081/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"123456"}'
```

**解决方案**：
- 检查网关配置：`stock-gateway/src/main/resources/application.yml`
- 确保所有服务都已启动
- 检查服务端口是否被占用

---

### 问题6：Redis连接失败

**现象**：登录时提示Redis连接错误

**排查步骤**：
```bash
# 1. 检查Redis是否启动
redis-cli ping
# 应该返回：PONG

# 2. 检查Redis配置
cat /Users/lifeng/Desktop/code/stock/stock-auth-service/src/main/resources/application.yml | grep redis
```

**解决方案**：
```bash
# 启动Redis
redis-server

# 或使用Docker启动
docker run -d -p 6379:6379 --name redis redis:5.0
```

---

### 问题7：CORS跨域错误

**现象**：浏览器控制台提示CORS错误

**排查步骤**：
```bash
# 查看网关CORS配置
cat /Users/lifeng/Desktop/code/stock/stock-gateway/src/main/java/com/stock/gateway/config/CorsConfig.java
```

**解决方案**：
- 确保网关的CORS配置正确
- 检查前端的 `vite.config.js` 代理配置
- 清除浏览器缓存

---

## 📊 测试检查清单

### 启动前检查
- [ ] MySQL已启动并初始化数据
- [ ] Redis已启动
- [ ] RabbitMQ已启动（可选）
- [ ] Java环境正常
- [ ] Maven环境正常
- [ ] Node.js环境正常

### 服务启动检查
- [ ] 认证服务启动成功 (8081)
- [ ] API网关启动成功 (8080)
- [ ] 前端启动成功 (5173)

### 登录功能检查
- [ ] 可以访问登录页面
- [ ] 表单验证正常
- [ ] 可以使用admin账号登录
- [ ] 可以使用test账号登录
- [ ] 登录后跳转到首页
- [ ] Token存储到LocalStorage
- [ ] Redis中存储了Token

### 数据验证
- [ ] MySQL中有用户数据
- [ ] 密码MD5值正确
- [ ] Redis中有Token记录
- [ ] 后端日志无错误

---

## 🎉 成功标志

当您看到以下现象时，说明登录功能完全正常：

1. ✅ 前端页面正常显示登录表单
2. ✅ 输入用户名密码后点击登录
3. ✅ 页面显示"登录成功"提示
4. ✅ 自动跳转到首页（中国地图页面）
5. ✅ 浏览器LocalStorage中存储了Token
6. ✅ Redis中存储了Token记录
7. ✅ 后端日志显示登录成功

---

## 📞 技术支持

如果遇到其他问题，请提供以下信息：

1. **错误现象**：详细描述问题
2. **浏览器控制台日志**：F12 → Console
3. **网络请求日志**：F12 → Network
4. **后端日志**：`stock-auth-service/logs/app.log`
5. **数据库数据**：`SELECT * FROM sys_user;`
6. **Redis数据**：`KEYS token:*`

---

## 🔗 相关文档

- [快速启动指南.md](快速启动指南.md)
- [API接口文档.md](API接口文档.md)
- [微服务架构说明.md](微服务架构说明.md)
- [部署文档.md](部署文档.md)

---

**祝您测试顺利！** 🎊
